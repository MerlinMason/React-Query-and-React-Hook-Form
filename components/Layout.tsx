const Layout: React.FC = ({ children }) => (
    <div className="bg-purple-800 min-h-screen">
        <div className="max-w-[600px] mx-auto py-12">{children}</div>
    </div>
);

export default Layout;
